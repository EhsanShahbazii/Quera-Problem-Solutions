using System;
using System.IO;
public class Main
{
    public static void Main(string[] args)
    {
        try (public const  scanner =  "Inputs";)
        {
            var number = Console.ReadLine();
            Console.WriteLine("saal:" + number[0].ToString() + number[1].ToString());
            Console.WriteLine("maah:" + number[2].ToString() + number[3].ToString());
        }
    }
}
